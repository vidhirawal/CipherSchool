// services/llm.service.js
export function buildHintPrompt(question, sql) {
  return `
You are a SQL tutor.
DO NOT give the final query.
ONLY give conceptual hints.

Question:
${question}

Student Query:
${sql}

Give a hint, not a solution.
`;
}
